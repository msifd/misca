package msifeed.misca.locks.client;

import msifeed.misca.Misca;
import net.minecraft.client.renderer.block.model.ModelResourceLocation;
import net.minecraft.util.ResourceLocation;
import net.minecraftforge.common.ForgeVersion;

public class ModelDynLock {
    public static final ModelResourceLocation LOCATION = new ModelResourceLocation(new ResourceLocation(Misca.MODID, "dynbucket"), "inventory");

}
