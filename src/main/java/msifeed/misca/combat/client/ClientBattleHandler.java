package msifeed.misca.combat.client;

import java.util.HashMap;
import java.util.HashSet;
import java.util.UUID;

public enum ClientBattleHandler {
    INSTANCE;

    public boolean selfCandidate = false;
//    public final HashSet<UUID> candidates = new HashSet<>();
}
